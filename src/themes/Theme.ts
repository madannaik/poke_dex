import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fontSizes: {
    sm: "20px",
  },
  colors: {
    white: "#e5e5e5",
    yellow: "#F5DB13;",
  },
});

export default theme;
